# Wealthora Capital monorepo — environment bootstrap
#
# Local:
#   cp apps/web/.env.example apps/web/.env.local
#   cp apps/api/.env.example apps/api/.env
#
# Render:
#   Use render.yaml Blueprint + dashboard env vars (see DEPLOYMENT.md)
#   Template checklist: env.render.example
#
# Never commit real secrets.

NODE_VERSION=22
PNPM_VERSION=9.15.0
