// Meridian FX — Prisma schema
// Full specification: docs/04-database-schema.md
// Scaffold only — models will be added in Phase 1.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
