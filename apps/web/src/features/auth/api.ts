/**
 * Auth feature API — thin re-export of domain service.
 * Hooks in `hooks.ts` should call these; components call hooks.
 */
export { authService as authApi } from '@/services/auth.service'
export { authService } from '@/services/auth.service'
