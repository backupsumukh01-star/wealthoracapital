/**
 * CMS feature API — thin re-export of domain service.
 * Hooks in `hooks.ts` should call these; components call hooks.
 */
export { cmsService as cmsApi } from '@/services/cms.service'
export { cmsService } from '@/services/cms.service'
